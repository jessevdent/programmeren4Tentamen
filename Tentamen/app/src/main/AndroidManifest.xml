<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jesse.tentamen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".LoginActivity"
                  android:label="Login">                     </activity>
        <activity android:name=".RegisterActivity"
                  android:label="Register">                  </activity>
        <activity android:name=".MovieActivity"
                  android:label="Available Movies">          </activity>
        <activity android:name=".DetailedActivity"
                  android:label="Selected Movie"
            android:parentActivityName=".MovieActivity">            </activity>
        <activity android:name=".RentalActivity"
                  android:label="Your Rentals">              </activity>
        <activity android:name=".DetailedRentalActivity"
                  android:label="Selected Rental">           </activity>


    </application>

</manifest>